#include <stdio.h>
#include <string.h>
struct SinhVien
{
	char MaSo[30];
	int Toan, Ly, Hoa;
};
typedef struct SinhVien SINHVIEN;

int main()
{
    SINHVIEN x;
    char name[30];
    int dtb;
    for(int i=0;i<5;i++)
    {
        struct SINHVIEN x1
        printf("\nNhap thong tin sinh vien thu %d",i+1);
        printf("\nNhap ten:");
        gets(name);
        printf("\nNhap vao diem toan: ");
	    scanf("%d", &x.Toan);
	    printf("\nNhap vao diem ly: ");
	    scanf("%d", &x.Ly);
	    printf("\nNhap vao diem hoa: ");
	    scanf("%d", &x.Hoa);
	    dtb=(x.Toan + x.Ly + x.Hoa) / 3;
	    printf("Ten sinh vien thu %d:",i+1);
	    puts(name);
	    printf("\nDiem toan: %d", x.Toan);
	    printf("\nDiem ly: %d", x.Ly);
	    printf("\nDiem hoa: %d", x.Hoa);
	    printf("\ndiem trung binh la: %d",dtb);
    }

    return 0;
}
